<lobbyUi:LobbyGui
    xmlns="https://spacestation14.io"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:maths="clr-namespace:Robust.Shared.Maths;assembly=Robust.Shared.Maths"
    xmlns:cc="clr-namespace:Content.Client.Administration.UI.CustomControls"
    xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
    xmlns:vote="clr-namespace:Content.Client.Voting.UI"
    xmlns:style="clr-namespace:Content.Client.Stylesheets"
    xmlns:lobbyUi="clr-namespace:Content.Client.Lobby.UI"
    xmlns:info="clr-namespace:Content.Client.Info"
    xmlns:widgets="clr-namespace:Content.Client.UserInterface.Systems.Chat.Widgets">
    <!-- Background -->
    <TextureRect Access="Public" VerticalExpand="True" HorizontalExpand="True" Name="Background"
                 Stretch="KeepAspectCovered" />
    <lobbyUi:LobbyTerminalBackground Name="TerminalBackground" Access="Public"
                                     VerticalExpand="True" HorizontalExpand="True"
                                     MouseFilter="Ignore" />
    <BoxContainer Name="MainContainer" VerticalExpand="True" HorizontalExpand="True" Orientation="Horizontal"
                  Margin="10 10 10 10" SeparationOverride="2">
        <SplitContainer State="Auto" ResizeMode="NotResizable" HorizontalExpand="True">
            <!-- LHS Controls -->
            <BoxContainer Name="LeftSide" Orientation="Vertical" SeparationOverride="4" HorizontalExpand="True">
                <BoxContainer Name="DefaultState" Orientation="Vertical"
                              VerticalExpand="True" HorizontalExpand="True">
                    <!-- Voting popups reserve space instead of covering primary actions. -->
                    <!-- <BoxContainer Orientation="Vertical" SeparationOverride="4" Name="VoteContainer"
                                  Access="Public" HorizontalAlignment="Left" VerticalAlignment="Top"
                                  Margin="8 8 8 4" /> -->

                    <Control VerticalExpand="True" HorizontalExpand="True">
                        <!-- Camera channel labels, inspired by the compact CM-SS13 lobby composition. -->
                        <BoxContainer Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Top"
                                      Margin="22 18 0 0" MouseFilter="Ignore">
                            <Label Text="{Loc 'lobby-terminal-camera'}" StyleClasses="LabelHeadingBigger" />
                            <Label Text="{Loc 'lobby-terminal-channel'}" StyleClasses="LabelSubText" />
                            <Label Name="TerminalSource" Access="Public" StyleClasses="LabelSubText" />
                        </BoxContainer>

                        <!-- Compact primary-action terminal. -->
                        <PanelContainer Name="TerminalCard" Access="Public" StyleClasses="CrtEmbeddedPanel"
                                        HorizontalAlignment="Left" VerticalAlignment="Center"
                                        MinWidth="320" MaxWidth="440">
                            <BoxContainer Orientation="Vertical" Margin="12 10 12 12" SeparationOverride="5">
                                <BoxContainer Name="TerminalHeader" Access="Public" Orientation="Vertical"
                                              HorizontalAlignment="Center">
                                    <Label Text="{Loc 'lobby-terminal-title'}" StyleClasses="LabelHeadingBigger"
                                           HorizontalAlignment="Center" />
                                    <Label Name="TerminalStatus" Access="Public"
                                           Text="{Loc 'lobby-terminal-status-waiting'}"
                                           StyleClasses="LabelBig" HorizontalAlignment="Center" />
                                    <Label Name="StartTime" Access="Public" Align="Center"
                                           FontColorOverride="{x:Static style:StyleNano.CrtGreenDim}"
                                           StyleClasses="LabelBig" HorizontalExpand="True" />
                                </BoxContainer>
                                <controls:HLine Color="{x:Static style:StyleNano.CrtGreenDim}" Thickness="1" />
                                <info:LinkBanner Name="LinkBanner" VerticalExpand="false" HorizontalExpand="True"
                                                 Margin="3 1 3 3" />
                                <BoxContainer Name="TerminalActions" Access="Public" Orientation="Vertical"
                                              SeparationOverride="5" HorizontalExpand="True">
                                    <Button Name="CharacterSetupQuickButton" Access="Public"
                                            Text="{Loc 'lobby-character-preview-panel-character-setup-button'}"
                                            StyleClasses="ButtonBig" HorizontalExpand="True" />
                                    <Button Name="OnboardingButton" Access="Public"
                                            Text="{Loc 'rmc-onboarding-lobby-button'}"
                                            StyleClasses="ButtonBig" HorizontalExpand="True" />
                                    <cc:UICommandButton Command="observe" Name="ObserveButton" Access="Public"
                                                        Text="{Loc 'ui-lobby-observe-button'}"
                                                        StyleClasses="ButtonBig" HorizontalExpand="True"
                                                        WindowType="{x:Type lobbyUi:ObserveWarningWindow}" />
                                    <Button Name="ReadyButton" Access="Public" ToggleMode="True"
                                            Text="{Loc 'ui-lobby-ready-up-button'}"
                                            StyleClasses="ButtonBig" HorizontalExpand="True" />
                                    <!-- RMC14/AU late-join channels -->
                                    <Button Name="JoinGovforButton" Access="Public"
                                            Text="{Loc 'rmc-lobby-join-govfor'}"
                                            StyleClasses="ButtonBig" HorizontalExpand="True" Visible="False" />
                                    <Button Name="JoinOpforButton" Access="Public"
                                            Text="{Loc 'rmc-lobby-join-opfor'}"
                                            StyleClasses="ButtonBig" HorizontalExpand="True" Visible="False" />
                                    <Button Name="JoinOtherButton" Access="Public"
                                            Text="{Loc 'rmc-lobby-join-other'}"
                                            StyleClasses="ButtonBig" HorizontalExpand="True" Visible="False" />
                                    <Button Name="JoinHuntButton" Access="Public"
                                            Text="{Loc 'rmc-lobby-join-hunt'}"
                                            StyleClasses="ButtonBig" MinWidth="137" Visible="False" />
                                </BoxContainer>
                                <RichTextLabel Name="PlaytimeComment" Visible="False" Access="Public"
                                               HorizontalAlignment="Center" />
                            </BoxContainer>
                        </PanelContainer>

                        <!-- Left Bot Panel -->
                        <PanelContainer VerticalAlignment="Bottom" HorizontalAlignment="Left"
                                        Margin="0 75" StyleClasses="CrtEmbeddedPanel" MaxWidth="720">
                            <RichTextLabel Name="LobbyMessageLabel" Access="Public" Margin="5" />
                        </PanelContainer>
                        <BoxContainer Orientation="Horizontal"
              HorizontalAlignment="Left"
              VerticalAlignment="Bottom">
    <info:DevInfoBanner Name="DevInfoBanner"
                        VerticalExpand="False"
                        Margin="3 3 3 3" />

    <PanelContainer StyleClasses="CrtEmbeddedPanel">
        <RichTextLabel Name="LobbySong"
                       Access="Public"
                       HorizontalAlignment="Center" />
    </PanelContainer>
</BoxContainer>

<!-- Голосования находятся внутри Control и не участвуют в разметке -->
<BoxContainer Name="VoteContainer"
              Access="Public"
              Orientation="Vertical"
              SeparationOverride="4"
              HorizontalAlignment="Left"
              VerticalAlignment="Top"
              Margin="40 75 0 0"
              MinWidth="340"
              MaxWidth="1000" />
                    </Control>
                </BoxContainer>
                <!-- Character setup state -->
                <!-- This is injected on startup. Funky! -->
                <Control Access="Public" Visible="False" Name="CharacterSetupState" VerticalExpand="True" />
            </BoxContainer>
            <!-- Right Panel -->
            <PanelContainer Name="RightSide" Access="Public" StyleClasses="CrtEmbeddedPanel" HorizontalAlignment="Right" VerticalExpand="True"
                            VerticalAlignment="Stretch">
                <BoxContainer Orientation="Vertical" HorizontalExpand="True">
                    <!-- Top row -->
                    <BoxContainer Orientation="Horizontal" MinSize="0 40" Name="HeaderContainer" Access="Public"
                                  SeparationOverride="4">
                        <Label Name="ServerName" Access="Public" StyleClasses="LabelHeadingBigger" VAlign="Center"
                               HorizontalExpand="True" HorizontalAlignment="Center" />
                    </BoxContainer>
                    <!-- Gold line -->
                    <controls:HLine Color="{x:Static style:StyleNano.CrtGreenDim}" Thickness="2" />
                    <controls:HSpacer Spacing="10" />
                    <!-- Voting & misc button bar -->
                    <BoxContainer Orientation="Horizontal" MinSize="0 40" HorizontalAlignment="Right">
                        <Button Name="AHelpButton" Access="Public" Text="{Loc 'ui-lobby-ahelp-button'}"
                                StyleClasses="ButtonBig" />
                        <vote:VoteCallMenuButton Name="CallVoteButton" StyleClasses="ButtonBig" />
                        <Button Name="OptionsButton" Access="Public" StyleClasses="ButtonBig"
                                Text="{Loc 'ui-lobby-options-button'}" />
                        <Button Name="LeaveButton" Access="Public" StyleClasses="ButtonBig"
                                Text="{Loc 'ui-lobby-leave-button'}" />
                    </BoxContainer>
                    <controls:HSpacer Spacing="10" />
                    <!-- Server info -->
                    <controls:NanoHeading Text="{Loc 'ui-lobby-server-info-block'}" />
                    <info:ServerInfo Name="ServerInfo" Access="Public" MinSize="0 30" VerticalExpand="false"
                           Margin="3 3 3 3" HorizontalAlignment="Left" />
                    <Label Name="StationTime" Access="Public" FontColorOverride="{x:Static maths:Color.LightGray}"
                           Margin="3 3 3 3" HorizontalAlignment="Left" />
                    <controls:HSpacer Spacing="5" />
                    <lobbyUi:LobbyCharacterPreviewPanel Name="CharacterPreview" Access="Public" />
                    <controls:HSpacer Spacing="5" />
                    <BoxContainer MinHeight="10" />
                    <!-- Gold line -->
                    <controls:HLine Color="{x:Static style:StyleNano.CrtGreenDim}" Thickness="2" Access="Public" />
                    <controls:HSpacer Spacing="10" />
                    <widgets:ChatBox Name="Chat" Access="Public" VerticalExpand="True" Margin="3 3 3 3" MinHeight="50" />
                </BoxContainer>
                <TextureButton Name="CollapseButton"
                                TexturePath="filled_right_arrow.svg.192dpi"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Top"
                                Margin="0 2 2 0"
                                ModulateSelfOverride="#9DFFB6"
                                Scale="0.5 0.5"/>
            </PanelContainer>
        </SplitContainer>
        <PanelContainer Name="ExpandPanel"
                        StyleClasses="CrtEmbeddedPanel"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Margin="0 2 2 0"
                        Visible="False">
            <TextureButton Name="ExpandButton"
                        TexturePath="filled_left_arrow.svg.192dpi"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        ModulateSelfOverride="#9DFFB6"
                        Scale="0.5 0.5"/>
        </PanelContainer>
    </BoxContainer>
    <BoxContainer Name="BalanceRatingContainer"
                  Access="Public"
                  Orientation="Vertical"
                  SeparationOverride="4"
                  HorizontalAlignment="Left"
                  VerticalAlignment="Bottom"
                  MouseFilter="Ignore" />
</lobbyUi:LobbyGui>
